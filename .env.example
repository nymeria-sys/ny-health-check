# URL do endpoint a ser monitorado
ENDPOINT_URL=http://app:80/health

# Intervalo de verificação em milissegundos (60000 = 1 minuto)
CHECK_INTERVAL_MS=60000

# Número de falhas consecutivas antes de reiniciar containers (padrão: 3)
MAX_FAILURES_BEFORE_RESTART=3

# Tipo de autenticação (deixe vazio, 'basic' ou 'bearer')
AUTH_TYPE=

# Credenciais para autenticação básica (AUTH_TYPE=basic)
AUTH_USERNAME=
AUTH_PASSWORD=

# Token para autenticação Bearer (AUTH_TYPE=bearer)
AUTH_TOKEN=

# Lista de containers para reiniciar (separados por vírgula)
# Exemplo: my-app,my-api,my-worker
CONTAINERS_TO_RESTART=my-app

# Caminho do socket do Docker (normalmente não precisa alterar)
DOCKER_SOCKET_PATH=/var/run/docker.sock
